<h1>An opinion is being carved in the stone tablet of history! How exciting!</h1>

<%= simple_form_for [@restaurant, @review] do |f|%>
<%= f.input :content %>
<%= f.input :rating %>
<%= f.submit class: 'btn btn-primary' %>
<% end %>
